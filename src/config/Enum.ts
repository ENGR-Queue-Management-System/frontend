export enum ROLE {
	ADMIN   = "Admin",
	STUDENT = "Student"
}

export enum STATUS {
  WAITING = "WAITING",
  IN_PROGRESS = "IN_PROGRESS",
  CALLED = "CALLED",
}

export enum DEVICE_TYPE {
  DESKTOP = "desktop",
  IOS = "ios",
  ANDROID = "android",
}
