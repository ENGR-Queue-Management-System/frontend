name: project_queue

services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: frontend
    restart: always
    ports:
      - 3000:80
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
